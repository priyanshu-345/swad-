// API Configuration
// This will be set via environment variable during build
// For local dev: leave empty (uses relative paths)
// For production: set to your Render backend URL
window.API_BASE_URL = '';

